/** Projection processing-worker adapter (L2) — barrel. */

export { startProjectionWorker } from './projection-worker.ts'
export type {
  ProjectionHandler,
  ProjectionHandlerContext,
  ProjectionWorkerDefinition,
  ProjectionWorkerOptions,
} from './projection-worker.ts'
