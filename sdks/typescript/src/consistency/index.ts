/** Consistency-on-dispatch wait (L3) — barrel. */

export {
  waitForProjection,
  DEFAULT_WAIT_POLL_INTERVAL_MS,
  DEFAULT_WAIT_TIMEOUT_MS,
} from "./consistency.ts";
export type {
  SubscriptionRef,
  WaitForProjectionOptions,
} from "./consistency.ts";
